import { formatNumber } from "@/lib/format";

export function StudentTable({ rows }: { rows: { name: string; nis: string; academicYear: string; branch: string; status: string; paymentDate: string }[] }) {
  return <section className="overflow-x-auto rounded-md border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 px-4 py-3"><h2 className="text-base font-semibold text-slate-950">Recent Students</h2><p className="mt-1 text-sm text-slate-500">20 most recent students by payment date.</p></div><table className="w-full min-w-[760px] border-collapse text-left text-sm"><thead className="bg-slate-100 text-xs uppercase text-slate-500"><tr>{["Name", "Student ID", "Academic Year", "Branch", "Status", "Payment Date"].map((label) => <th key={label} className="px-3 py-2 font-semibold">{label}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row) => <tr key={row.nis} className="transition-colors hover:bg-slate-50"><td className="px-3 py-2 font-medium text-slate-800">{row.name}</td><td className="px-3 py-2 font-mono text-xs text-slate-500">{row.nis}</td><td className="px-3 py-2 text-slate-600">{row.academicYear}</td><td className="px-3 py-2 text-slate-600">{row.branch}</td><td className="px-3 py-2 text-slate-600">{row.status}</td><td className="px-3 py-2 text-slate-600">{row.paymentDate}</td></tr>)}{!rows.length ? <tr><td colSpan={6} className="px-3 py-8 text-center text-slate-500">No student data available.</td></tr> : null}</tbody></table><div className="border-t border-slate-200 px-4 py-2 text-xs text-slate-500">{formatNumber(rows.length)} rows displayed</div></section>;
}
